1. enum
=> 상수들의 집합을 의미하는 TS 자료형이다.


2. enum 특징
2-1. enum enumName {...};

1-2. enum은 기본적으로 0부터 시작해서 멤버들의 번호를 매긴다.
1-3. 0부터 시작할 필요 x => 값을 할당하면 그 값부터 +1로 증가하여 매겨진다.
1-4. 인덱스를 통해 key를 알아낼 수 있다. => 역방향 매핑

1-5. 문자열로 통해 초기화할 수 있다. 다만 이 떄는 초기화를 다 해줘야 한다.



